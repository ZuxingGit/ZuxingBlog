---
import Layout from '../layouts/Layout.astro';

const zeroOrOneOrTwo = () => {
  return Math.round(Math.random()) * 2;
};

const blog =
  ['B', 'b', '🅱'][zeroOrOneOrTwo()] +
  ['L', 'l', 'ℒ'][zeroOrOneOrTwo()] +
  ['O', '𝓞', 'Ó'][zeroOrOneOrTwo()] +
  ['G', 'g', '𝕘'][zeroOrOneOrTwo()];
---

<Layout>
  <h1 class="text-center text-5xl sm:text-9xl pt-[36dvh] sm:pt-[30dvh]">
    <span class="font-bold dark:text-white">Zuxing's</span>
    <span
      class="font-extrabold bg-clip-text text-transparent bg-gradient-to-br from-[#ff5ef7] to-[#02f5ff]"
    >
      {blog}
    </span>
  </h1>
</Layout>
